<h1>Monsters#index</h1>
<p>
  <% @monsters.each do |monster| %>
    <div class="card-small">
      <p><%= monster.name %></p>
      <p><%= monster.description %></p>
    </div>
    <%= button_to 'KILL', monster_path(monster), method: :delete, data: { confirm: 'Are you sure?' } %>
  <% end %>
</p>
