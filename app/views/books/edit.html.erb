<div data-controller="toggle" class="m-5 text-center">
  <button data-toggle-target="button" data-action="click->toggle#fire" class="btn btn-outline-primary">See the Edit Form</button>
  <div class="d-none" data-toggle-target="togglableElement">
    <%= render "shared/book_form", book: @book %>
  </div>
</div>
